{
  "name": "mod_current_events",
  "version": "1.2.0",
  "description": "Joomla Module zur Anzeige aktueller Veranstaltungen",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/weltspiegel-cottbus/mod_current_events.git"
  },
  "type": "module",
  "scripts": {
    "build": "echo 'No assets to build for this module'",
    "package": "node .build/package.mjs",
    "release": "npm run build && npm run package",
    "changelog": "changelogen --output CHANGELOG.md --noAuthors",
    "release:patch": "changelogen --release --patch --push --noAuthors",
    "release:minor": "changelogen --release --minor --push --noAuthors",
    "release:major": "changelogen --release --major --push --noAuthors"
  },
  "devDependencies": {
    "changelogen": "^0.6.2"
  }
}
